[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: needs Ollama and/or an ingested knowledge base
filterwarnings =
    ignore::DeprecationWarning
